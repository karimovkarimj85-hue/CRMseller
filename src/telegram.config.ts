/**
 * Telegram: заявки с формы → канал.
 * 1. @BotFather → /newbot или /token → скопируйте токен сюда
 * 2. Добавьте бота админом в канал (право «Публиковать сообщения»)
 * 3. ID канала: перешлите любой пост из канала в @userinfobot или @getidsbot
 */
export const telegramBotToken = '';
export const telegramChatId = '-1003692525683';
